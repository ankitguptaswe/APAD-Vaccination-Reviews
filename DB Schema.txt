CREATE TABLE USER(
   USER_ID        INTEGER     PRIMARY KEY     NOT NULL,
   NAME           TEXT    NOT NULL,
   EMAIL          TEXT    NOT NULL,
   THEMES         TEXT    NOT NULL,
   PICTURE        TEXT
);

CREATE TABLE HOSPITAL(
   HOSPITAL_ID    INTEGER     PRIMARY KEY     NOT NULL,
   NAME           TEXT    NOT NULL,
   LOCATION       TEXT    NOT NULL,
   PICTURE        TEXT
);

CREATE TABLE PHARMACY(
   PHARMACY_ID    INTEGER     PRIMARY KEY     NOT NULL,
   NAME           TEXT    NOT NULL,
   LOCATION       TEXT    NOT NULL,
   PICTURE        TEXT
);

CREATE TABLE VACCINE(
   VACCINE_ID     INTEGER     PRIMARY KEY     NOT NULL,
   NAME           TEXT    NOT NULL,
   LOCATION       TEXT    NOT NULL,
   PICTURE        TEXT
);

CREATE TABLE REVIEWS(
   REVIEW_ID      INTEGER     PRIMARY KEY     NOT NULL,
   USER_ID        INTEGER                     NOT NULL,
   TITLE          TEXT                    NOT NULL,
   THEME          TEXT                    NOT NULL,
   RATING         INTEGER                     NOT NULL,
   PICTURE        TEXT                    NOT NULL,
   DESCRIPTION    TEXT                    NOT NULL,
   TAGS           TEXT                    NOT NULL
);

CREATE TABLE THEMES(
   THEME_NAME     TEXT     PRIMARY KEY     NOT NULL,
   PICTURE        TEXT,
   DESCRIPTION    TEXT
);

INSERT INTO THEMES(THEME_NAME, DESCRIPTION)
VALUES('pharmacy', 'Pharmacies which the user can visit for treatment/vaccination')

INSERT INTO USER (USER_ID,NAME,THEMES)
VALUES (1, 'Asad Aali', 'Vaccine');

INSERT INTO USER (USER_ID,NAME,THEMES)
VALUES (2, 'Ankit Gupta', 'All');

INSERT INTO PHARMACY (PHARMACY_ID,NAME,LOCATION)
VALUES (1, 'CVS Pharmacy', 'Austin, TX');

INSERT INTO HOSPITAL (HOSPITAL_ID,NAME,LOCATION)
VALUES (1, 'Dell Seton', 'Austin, TX');

INSERT INTO VACCINE (VACCINE_ID,NAME,MANUFACTURER,LOCATION)
VALUES (1, 'Pfizer BioNTech', 'BioNTech', 'Austin, TX');
