CREATE TABLE USER(
   USER_ID        INT     PRIMARY KEY     NOT NULL,
   NAME           TEXT    NOT NULL,
   THEMES         TEXT    NOT NULL,
   PICTURE        BLOB
);

CREATE TABLE HOSPITAL(
   HOSPITAL_ID    INT     PRIMARY KEY     NOT NULL,
   NAME           TEXT    NOT NULL,
   LOCATION       TEXT    NOT NULL,
   PICTURE        BLOB
);

CREATE TABLE PHARMACY(
   PHARMACY_ID    INT     PRIMARY KEY     NOT NULL,
   NAME           TEXT    NOT NULL,
   LOCATION       TEXT    NOT NULL,
   PICTURE        BLOB
);

CREATE TABLE VACCINE(
   VACCINE_ID     INT     PRIMARY KEY     NOT NULL,
   NAME           TEXT    NOT NULL,
   MANUFACTURER   TEXT    NOT NULL,
   LOCATION       TEXT    NOT NULL,
   PICTURE        BLOB
);

CREATE TABLE REVIEWS(
   REVIEW_ID      INT     PRIMARY KEY     NOT NULL,
   USER_ID        INT                     NOT NULL,
   TITLE          TEXT                    NOT NULL,
   THEME          TEXT                    NOT NULL,
   HOSPITAL_ID    INT,
   PHARMACY_ID    INT,
   VACCINE_ID     INT,
   RATING         INT                     NOT NULL,
   PICTURE        BLOB                    NOT NULL,
   DESCRIPTION    TEXT                    NOT NULL,
   TAGS           TEXT                    NOT NULL
);

INSERT INTO USER (USER_ID,NAME,THEMES)
VALUES (1, 'Asad Aali', 'Vaccine');

INSERT INTO USER (USER_ID,NAME,THEMES)
VALUES (2, 'Ankit Gupta', 'All');

INSERT INTO PHARMACY (PHARMACY_ID,NAME,LOCATION)
VALUES (1, 'CVS Pharmacy', 'Austin, TX');

INSERT INTO HOSPITAL (HOSPITAL_ID,NAME,LOCATION)
VALUES (1, 'Dell Seton', 'Austin, TX');

INSERT INTO VACCINE (VACCINE_ID,NAME,MANUFACTURER,LOCATION)
VALUES (1, 'Pfizer BioNTech', 'BioNTech', 'Austin, TX');
